# Stage B Cyclic Eigenstrain Plan

Purpose: test dynamic loading rather than a static eigenstrain hold.

Target design:

- same B3 no-signal geometry;
- eps cycle `0 -> 0.010 -> 0`;
- repeat cycles after smoke validation;
- production remains 100k-class.

Current blocker: cyclic eigenstrain schedules are not implemented in the
current input generator.

