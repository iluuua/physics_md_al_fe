# Stage B Seeded Defect Plan

Purpose: test whether eigenstrain moves or amplifies an existing defect rather
than nucleating from a perfect starting point.

Target design:

- 100k-class near-GB inclusion system;
- seeded dislocation near the inclusion/GB;
- same eps choices as Stage B realism;
- smoke before production.

Current blocker: `seed_dislocation_if_available` is explicitly rejected until a
real seeding implementation exists.

